providers:
  my-provider:
    base_url: https://api.example.com/v1
    models:
      - model-name

  # another-provider:
  #   base_url: https://api.another.com/v1
  #   models:
  #     - another-model

server:
  host: 0.0.0.0
  port: 4000
  api_port: 4001

db:
  path: ~/.llm-tracker/usage.db
